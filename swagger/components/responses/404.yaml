description: There are no contacts in the notebook yet!
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
      properties:
        status:
          type: integer
          example: 404
        message:
          type: string
          example: There are no contacts in the notebook yet!
